#include <stdio.h>
#include <math.h>
#include "lab_math.h"
#include <stdbool.h>

int main() {
    double a, b, c,perometr, plashad;
    
    printf("vvedite znachenie A: ");
        scanf("%lf", &a);
    printf("vvedite znachenie B: ");
        scanf("%lf", &b);
    printf("vvedite znachenie C: ");
        scanf("%lf", &c);
     
    if(istengel(a,b,c)){
        perometr = P(a, b, c);
        plashad = S(a, b, c); 
        
    printf("perimetr triugolnica %.3lf\n",perometr);    
    printf("plashad triugolnica %.3lf\n",plashad);    
    }
    else {
    printf("treugolnica ne suchestvuet.\n");
    }
        return 0;
    
}
