/*Объявить тип-структуру humen, включающую имя человека, фамилию и год рождения. Объявить два массива из четырёх элементов 
типа humen. Ввести с консоли или из файла элементы одного массива и построить на их основе другой, 
упорядочив элементы по годам рождения. Вывести результат.*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct humen {
    char name[50];
    char surname[50];
    char birth_year[10];
};

int main() {
    struct humen people[4];
    int i;
    for (i = 0; i < 4; i++) {
        printf("Enter person %d:\n", i + 1);
        printf("name: ");
        scanf("%s", people[i].name);
        printf("last name: ");
        scanf("%s", people[i].surname);
        printf("birth_year: ");
        scanf("%s", people[i].birth_year);
    }

    printf("\nUnsorted:\n");
    for (i = 0; i < 4; ++i) {
        printf("%s %s, birth_year: %s\n", people[i].name, people[i].surname, people[i].birth_year);
    }

    printf("\nFiltered by birth year (1990-2000):\n");
    for (i = 0; i < 4; ++i) {
        int birth_year = atoi(people[i].birth_year);
        if (birth_year >= 1990 && birth_year <= 2000) {
            printf("%s %s, birth_year: %s\n", people[i].name, people[i].surname, people[i].birth_year);
        }
    }

    return 0;
}












