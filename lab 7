#include <stdio.h>
#include <stdlib.h>
#include <string.h>


struct humen {
    char name[50];
    char surname[50];
    int birth_year;
}

int main() {
    
struct humen people[4];
	
	
    
for (int i = 0; i < 4; i++) {
    printf("vvedite ima cheloveka %d:\n", i + 1);
    printf("name: ");
    scanf("%s", people[i].name);
    printf("last name: ");
    scanf("%s", people[i].surname);
    printf("birth_year: ");
    scanf("%d", &people[i].birth_year);
}

    qsort(people, 4, sizeof(struct humen), compare_years);

   
    printf("\notsortirovana:\n");
    for (int i = 0; i < 4; ++i) {
        printf("%s %s, birth_year: %d\n", people[i].name, people[i].surname, people[i].birth_year);
    }

    return 0;
}
